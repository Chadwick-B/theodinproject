/*

  Clean up CSS by importing styles in the components, probably in the index.js of each
  initialize cards array as empty, figure that out lmao
  Decouple CardGrid and Score components, combine them in a larger overall component, then export that
  Then make Main include all of the components
  Then make App.js import main

*/
